# ============================================================
# SAPDE — Dependencias Python 3.12
# Instalar: py -3.12 -m pip install -r requirements.txt
# ============================================================

# --- Ciencia de datos y ML ---
numpy>=1.26.4,<2.1
pandas>=2.2.2,<3.0
scipy>=1.13.0
scikit-learn>=1.5.0
lightgbm>=4.3.0
imbalanced-learn>=0.12.3
joblib>=1.4.2
pyarrow>=16.0.0         # soporte Parquet
fastparquet>=2024.2.0

# --- Optimización de hiperparámetros ---
optuna>=3.6.0

# --- Interpretabilidad ---
shap>=0.45.0

# --- Visualización ---
matplotlib>=3.9.0
seaborn>=0.13.2
plotly>=5.22.0

# --- Dashboard interactivo (OE4) ---
streamlit>=1.35.0

# --- API Backend ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
gunicorn>=22.0.0        # sólo Linux/Mac; en Windows usar uvicorn directamente
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Base de datos ---
sqlalchemy>=2.0.30
psycopg2-binary==2.9.10  # versión fijada: compatible con Python 3.12

# --- Logging estructurado ---
loguru>=0.7.2

# --- ANFIS / Sistema Neurodifuso (OE2) ---
# Opción A: TensorFlow (soporta Python 3.12 desde v2.16)
# tensorflow>=2.16.1
# Opción B (recomendada en Windows): instalar manualmente si TF falla
#   pip install torch torchvision  -- para implementación PyTorch de ANFIS
# La implementación ANFIS se activa en src/sapde/models/anfis.py
# Si ninguna opción compila, se usa scikit-fuzzy como fallback documentado
scikit-fuzzy>=0.4.2

# --- Paralelización Python fallback ---
numba>=0.59.0           # JIT compilation, fallback si C++/OpenMP no compila

# --- Utilidades ---
python-dotenv>=1.0.1
tqdm>=4.66.4
click>=8.1.7
